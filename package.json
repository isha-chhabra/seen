{
  "name": "seen",
  "version": "0.2.13",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "biome check --diagnostic-level=error --config-path=biome.json .",
    "lint:fix": "biome check --write --diagnostic-level=error --config-path=biome.json .",
    "test": "turbo test",
    "format": "biome format --write --config-path=biome.json .",
    "knip": "knip",
    "license-check": "node scripts/check-licenses.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.8",
    "knip": "^6.32.2",
    "tsx": "^4.23.12",
    "turbo": "^2.10.10",
    "typescript": "^7.0.2"
  }
}
