/**
 * Sidebar footer app-info slot.
 *
 * Rebranded self-host: the upstream version string and the website / GitHub
 * links are intentionally removed. Kept as a no-op component so callers and
 * layout stay untouched.
 */
export function NavAppInfo() {
	return null;
}
