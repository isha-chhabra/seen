/**
 * Sidebar footer app-info slot.
 *
 * Rendered as a no-op in this build: no version string or external links.
 * Kept so callers and layout stay untouched.
 */
export function NavAppInfo() {
	return null;
}
